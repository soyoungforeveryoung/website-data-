# 바 솝 2종 공통 — 코어 사실

바 솝 2종(뱀부 포레스트 차콜 디톡스 바 · 부시맨 캔들 미르 프로텍션 바)에
**동일하게 적용되는 사실**만 담는다. 제품별로 다른 값은 각 제품 파일에 있다.

- 최종 갱신: 2026-09-10
- 원칙: **추측한 값은 넣지 않는다.** 확인하지 못한 항목은 `확인 필요`로 남긴다.

---

## 0. 전성분은 여기서 받는다 🔴

| 항목 | 값 | 출처 |
|---|---|---|
| 바 솝 제조 | **코보** 비누공장 | `소영` (2026-09-13) |
| 장인 | **손용석** — **비누 장인** | `소영` (2026-09-13) |
| 정식 상호 · 연락처 | `확인 필요` | — |

**바 솝의 전성분(INCI 전체 목록)은 비누공장에 물어보면 나온다** (`소영`).

📌 **손용석 장인은 브랜드 스토리 자산이기도 하다.**
   죽염(영월 최규동 장인)과 같은 구조다 — *누가 만들었는가*가 있는 제품이다.
   다만 **표기 방식은 소영님 확정 전까지 쓰지 않는다** (`확인 필요`):
   장인 성함·공방명을 상세페이지에 노출할지, 노출한다면 어떤 문구로 할지.

→ 향료사(AFM·DreamAir·Chemia)에 **전성분이나 배합률을 물을 필요가 없다.**
  향료사는 공식적으로 전성분을 주지 않으며(→ `docs/fragrance-registry.md`),
  우리가 필요한 값은 **완제품 기준**이지 향료 내부 조성이 아니다.

### 이 한 건으로 풀리는 것

| 미해결 항목 | 비누공장 전성분으로 해결되나 |
|---|---|
| 사이트 전성분 표기 정확도 | ✅ 이게 정답지다 |
| `Commiphora myrrha` → `Commiphora Wildii Oil` 정정 | ✅ 확정 가능 |
| 향료 배합률 (알레르겐 계산용) | ✅ 완제품 기준 값이 나온다 |
| 알레르겐 표시 문구 | ✅ 한국 라벨용으로 이미 계산돼 있을 가능성 높음 |
| 파라벤·SLS 등 무첨가 표시 | ✅ 완제품 기준 확인 |

⚠️ **캔들은 해당 없다.** 캔들 제조는 Stone Candles(Christia)다. 공장이 다르다.

---

## 1. 중량

| 항목 | 값 | 출처 |
|---|---|---|
| 순 중량 | **255 g (9 oz)** | `소영` (2026-09-11 확정) |

2종 모두 255 g으로 동일하다.

⚠️ **캔들(250 g)과 값이 다르다.** 바 솝만 255 g이다. 세트 표기 시 혼동하지 않도록 주의.

`255 g = 8.99 oz`이므로 **`9 oz`는 정확한 표기다.** 캔들과 달리 바 솝은
박스 인쇄값을 고칠 필요가 없다.

표기 형식: **`255 g (9 oz)`**

### 🔴 Shopify 등록 중량 오류

| 제품 | 현재 등록값 | 조치 |
|---|---|---|
| 차콜 바 솝 | 0.6 lb (272 g) | 255 g + 포장 기준이면 타당. 확인만 |
| 미르 프로텍션 바 | **0.2 lb (약 91 g)** | 🔴 명백한 오류. **255 g 기준으로 수정** |

---

## 2. 제조

| 항목 | 값 | 출처 |
|---|---|---|
| 제조 국가 | **한국** | `소영` |
| 제조 방식 | 비누 장인이 손으로 제조 후 자연 건조 | `소영` |
| 건조 기간 | **약 1개월** | `소영` |
| 장인 이름·공방명 | **비공개** | `소영` (공개하지 않기로 확정) |

⚠️ 캔들은 미국, 바 솝은 한국이다. 세트 상품 표기 시 원산지를 각각 명시해야 한다.

⚠️ `US-RAW` 미르 프로텍션 바 본문에 `Hand-poured by our artisan soap maker`라고 쓰여 있다.
   `hand-poured`는 캔들 용어다. 바 솝은 `handmade` / `손으로 만든`으로 수정 대상.

---

## 3. 로프

| 항목 | 값 | 출처 |
|---|---|---|
| 형태 | 로프가 달린 바 솝 (soap on a rope) | `소영`, `US-RAW` `soap-on-a-rope` |
| 소재 | 린넨 혼방 | `US-RAW` `hanging-linen composition natural rope` |
| 생분해 | 생분해됨 | `US-RAW` `the rope is biodegradable` |

기능 (사실만):
1. 샤워실에 걸어둘 수 있다
2. 비누 받침이 필요 없다
3. 물에 잠기지 않는다
4. 그래서 더 오래 쓴다

---

## 4. 가격

| 시장 | 값 | 출처 |
|---|---|---|
| 미국 | USD 35.00 | `US-RAW` variant price (2종 동일) |
| 한국 | KRW 38,000 | `KR-HTML` JSON-LD offers (2종 동일) |

---

## 5. 사용법

| 항목 | 값 | 출처 |
|---|---|---|
| 사용 | 물에 적셔 거품을 내어 사용 | `US-RAW` pdp_how_to_use |
| 보관 | 사용 후 로프에 걸어 자연 건조 | `US-RAW` `hang the soap by its rope to air-dry` |

---

## 6. 표시사항 — 확인 필요

⚠️ 바 솝은 **화장품**이다. 캔들과 규제 체계가 완전히 다르다.

| 항목 | 상태 |
|---|---|
| 한국 화장품법 전성분 표기 (인체세정용 제품류) | **확인 필요** |
| EU 화장품규정 1223/2009 CPNP 등록 · Responsible Person | **확인 필요** |
| EU 알레르겐 표시 (규정 2023/1545 확대 대응) | **확인 필요** |
| 미국 MoCRA 시설 등록 · 제품 리스팅 | **확인 필요** |
| 견과류(sweet almond) 알레르기 고지 — 미르 프로텍션 바 | **확인 필요** |

---

# 확인 필요 항목 (바 솝 공통)

| # | 항목 | 필요한 것 | 상태 |
|---|---|---|---|
| B1 | ~~oz 표기~~ | ✅ `255 g (9 oz)` 확정 | 해결 |
| B2 | Shopify 미르 등록 중량 | 0.2 lb → 255 g | 🔴 수정 필요 |
| B3 | 화장품 표시·등록 요건 | 한국·EU·미국 각각 확인 | 대기 |
| B4 | 향료 알레르겐 선언서 | 향료사 | 대기 |
